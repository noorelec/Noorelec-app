rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // PDFs des devis par utilisateur
    match /devis_pdfs/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}

